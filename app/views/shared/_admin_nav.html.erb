<nav class="bg-white shadow-sm">
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <div class="flex h-16 justify-between">
      <div class="flex">
        <div class="flex flex-shrink-0 items-center">
          <%= link_to admin_root_path, class: "text-lg font-semibold text-emerald-600" do %>
            VeloTrain Admin
          <% end %>
        </div>
        <div class="hidden sm:-my-px sm:ml-6 sm:flex sm:space-x-8">
          <%= link_to admin_root_path,
              class: "inline-flex items-center border-b-2 px-1 pt-1 text-sm font-medium #{current_page?(admin_root_path) ? 'border-emerald-500 text-gray-900' : 'border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700'}" do %>
            Dashboard
          <% end %>

          <%= link_to admin_gpx_tracks_path,
              class: "inline-flex items-center border-b-2 px-1 pt-1 text-sm font-medium #{request.path.start_with?('/admin/gpx') ? 'border-emerald-500 text-gray-900' : 'border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700'}" do %>
            GPX Tracks
          <% end %>

          <%= link_to admin_areas_path,
              class: "inline-flex items-center border-b-2 px-1 pt-1 text-sm font-medium #{current_page?(admin_areas_path) ? 'border-emerald-500 text-gray-900' : 'border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700'}" do %>
            Areas
          <% end %>

          <%= link_to admin_regional_bike_rules_path,
              class: "inline-flex items-center border-b-2 px-1 pt-1 text-sm font-medium #{current_page?(admin_regional_bike_rules_path) ? 'border-emerald-500 text-gray-900' : 'border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700'}" do %>
            Bike Rules
          <% end %>

          <a href="/admin/jobs"
             class="inline-flex items-center border-b-2 px-1 pt-1 text-sm font-medium <%= request.path.start_with?('/admin/jobs') ? 'border-emerald-500 text-gray-900' : 'border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700' %>">
            Jobs
          </a>
        </div>
      </div>

      <div class="hidden sm:ml-6 sm:flex sm:items-center">
        <div class="relative ml-3">
          <%= link_to root_path,
              class: "rounded-md bg-white px-3 py-2 text-sm font-semibold text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 hover:bg-gray-50" do %>
            ← Back to Site
          <% end %>
        </div>
      </div>

      <!-- Mobile menu button -->
      <div class="-mr-2 flex items-center sm:hidden">
        <button type="button"
                class="relative inline-flex items-center justify-center rounded-md bg-white p-2 text-gray-400 hover:bg-gray-100 hover:text-gray-500 focus:outline-none focus:ring-2 focus:ring-emerald-500 focus:ring-offset-2"
                aria-controls="mobile-menu"
                aria-expanded="false"
                data-controller="dropdown"
                data-action="click->dropdown#toggle">
          <span class="absolute -inset-0.5"></span>
          <span class="sr-only">Open main menu</span>
          <svg class="block h-6 w-6" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" />
          </svg>
        </button>
      </div>
    </div>
  </div>

  <!-- Mobile menu -->
  <div class="hidden sm:hidden" id="mobile-menu" data-dropdown-target="menu">
    <div class="space-y-1 pb-3 pt-2">
      <%= link_to admin_dashboard_path,
          class: "block border-l-4 py-2 pl-3 pr-4 text-base font-medium #{current_page?(admin_dashboard_path) ? 'border-emerald-500 bg-emerald-50 text-emerald-700' : 'border-transparent text-gray-600 hover:border-gray-300 hover:bg-gray-50 hover:text-gray-800'}" do %>
        Dashboard
      <% end %>

      <%= link_to admin_gpx_tracks_path,
          class: "block border-l-4 py-2 pl-3 pr-4 text-base font-medium #{request.path.start_with?('/admin/gpx') ? 'border-emerald-500 bg-emerald-50 text-emerald-700' : 'border-transparent text-gray-600 hover:border-gray-300 hover:bg-gray-50 hover:text-gray-800'}" do %>
        GPX Tracks
      <% end %>

      <%= link_to admin_areas_path,
          class: "block border-l-4 py-2 pl-3 pr-4 text-base font-medium #{current_page?(admin_areas_path) ? 'border-emerald-500 bg-emerald-50 text-emerald-700' : 'border-transparent text-gray-600 hover:border-gray-300 hover:bg-gray-50 hover:text-gray-800'}" do %>
        Areas
      <% end %>

      <%= link_to admin_regional_bike_rules_path,
          class: "block border-l-4 py-2 pl-3 pr-4 text-base font-medium #{current_page?(admin_regional_bike_rules_path) ? 'border-emerald-500 bg-emerald-50 text-emerald-700' : 'border-transparent text-gray-600 hover:border-gray-300 hover:bg-gray-50 hover:text-gray-800'}" do %>
        Bike Rules
      <% end %>

      <a href="/admin/jobs"
         class="block border-l-4 py-2 pl-3 pr-4 text-base font-medium <%= request.path.start_with?('/admin/jobs') ? 'border-emerald-500 bg-emerald-50 text-emerald-700' : 'border-transparent text-gray-600 hover:border-gray-300 hover:bg-gray-50 hover:text-gray-800' %>">
        Jobs
      </a>

      <%= link_to root_path,
          class: "block border-l-4 border-transparent py-2 pl-3 pr-4 text-base font-medium text-gray-600 hover:border-gray-300 hover:bg-gray-50 hover:text-gray-800" do %>
        ← Back to Site
      <% end %>
    </div>
  </div>
</nav>
